services:
  app:
    image: python:3.12
    working_dir: /app
    volumes:
      - ./app:/app
    ports:
      - "8080:8080"
    command: bash -c "pip install -r requirements.txt && python manage.py runserver 0.0.0.0:8080"